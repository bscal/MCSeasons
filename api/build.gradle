plugins {
    id 'fabric-loom'
    id 'maven-publish'
    id 'net.ltgt.errorprone'
}

apply plugin: 'java'

archivesBaseName = rootProject.archives_base_name + "_api"

dependencies {
    implementation project(path: rootProject.path, configuration: "namedElements")
}

test {
    //useJUnitPlatform()
}