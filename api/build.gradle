plugins {
    id 'fabric-loom'
    id 'maven-publish'
    id 'net.ltgt.errorprone'
}

archivesBaseName = rootProject.archives_base_name + "_api"

apply plugin: 'java'

dependencies {
    implementation(rootProject)
}